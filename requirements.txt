# Núcleo de dados e modelagem
pandas==2.2.3
numpy==2.0.2
scikit-learn==1.5.2
joblib==1.4.2

# Ingestão documental e recuperação semântica (ADR-009)
pypdf==5.1.0
pypdfium2==5.12.1
rapidocr-onnxruntime==1.4.4
chromadb==1.5.9
sentence-transformers==3.3.1

# Camada de prescrição (ADR-001)
ollama==0.4.4

# API (ADR-002)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
# Fixado em 0.27.x por exigência do cliente do Ollama (`httpx<0.28.0,>=0.27.0`). É a
# versão que o ambiente de desenvolvimento sempre usou; o arquivo é que declarava 0.28.1
# e tornava `pip install -r requirements.txt` irresolúvel num ambiente limpo.
httpx==0.27.2

# A interface é um projeto separado em web/ (React + Vite + TypeScript), que consome esta
# API por HTTP. Ver web/README.md.

# Análise exploratória e validação dos modelos (notebooks/)
# Não são dependências de execução: a API e a interface não importam nada daqui. Ficam
# neste arquivo, e não em um separado, porque o notebook do ADR-003 é material de defesa
# da solução — quem clonar precisa conseguir reexecutá-lo sem procurar instruções.
matplotlib==3.9.2
nbformat==5.10.4
nbconvert==7.16.4
ipykernel==6.29.5

# Testes
pytest==8.3.4
